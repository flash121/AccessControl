import numpy as np
import pandas as pd

# read in csv and return header and content seperately
def readCSV(filename):
    content = pd.read_csv(filename)
    print content[:1]
    return content

def parseContent(content):
    res = content['resource'] == id
    d = content(content['resource'] == id)
    data = content.__delitem__("resource")
    return id, data

d=data((data(:,2)==id),:);
access=d(:,1);
path=d(:,2:end);
def resource(id, data):
    pass


readCSV('train.csv')
    